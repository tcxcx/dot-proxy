import { Logger } from '@subsquid/logger';
import { RpcClient } from '@subsquid/rpc-client';
import { ExploreApi } from './explore';
import { SpecVersion, SpecVersionRecord } from './specVersion';
export declare class RpcApi implements ExploreApi {
    private client;
    private log?;
    constructor(client: RpcClient, log?: Logger | undefined);
    getHeight(): Promise<number>;
    getVersionRecord(height: number): Promise<SpecVersionRecord>;
    getVersionRecords(fromBlock?: number, toBlock?: number): Promise<SpecVersionRecord[]>;
    getVersionRecordArray(heights: number[]): Promise<SpecVersionRecord[]>;
    getVersion(rec: SpecVersionRecord): Promise<SpecVersion>;
    getSingleVersionRecord(): Promise<SpecVersionRecord | undefined>;
}
//# sourceMappingURL=rpc-api.d.ts.map