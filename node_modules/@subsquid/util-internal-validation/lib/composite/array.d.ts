import { ValidationFailure } from '../error';
import { Validator } from '../interface';
export declare class ArrayValidator<T, S> implements Validator<T[], S[]> {
    readonly item: Validator<T, S>;
    constructor(item: Validator<T, S>);
    cast(array: unknown): ValidationFailure | T[];
    validate(array: unknown): ValidationFailure | undefined;
    phantom(): S[];
}
//# sourceMappingURL=array.d.ts.map