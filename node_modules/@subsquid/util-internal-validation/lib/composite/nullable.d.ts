import { ValidationFailure } from '../error';
import { Validator } from '../interface';
export declare class NullableValidator<T, S> implements Validator<T | null, S | null> {
    readonly value: Validator<T, S>;
    constructor(value: Validator<T, S>);
    cast(value: unknown): ValidationFailure | T | null;
    validate(value: unknown): ValidationFailure | undefined;
    phantom(): S | null;
}
//# sourceMappingURL=nullable.d.ts.map