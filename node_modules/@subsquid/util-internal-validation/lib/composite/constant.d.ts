import { ValidationFailure } from '../error';
import { Validator } from '../interface';
export declare class ConstantValidator<T> implements Validator<T> {
    readonly value: T;
    readonly equals: (a: unknown, b: T) => boolean;
    private message;
    constructor(value: T, equals?: (a: unknown, b: T) => boolean);
    cast(value: unknown): ValidationFailure | T;
    validate(value: unknown): ValidationFailure | undefined;
    phantom(): T;
}
//# sourceMappingURL=constant.d.ts.map