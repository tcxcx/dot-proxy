import { Logger } from '@subsquid/logger';
import { GetPatternParams } from './path-pattern';
import { HttpResource } from './resource';
import { ListeningServer } from './server';
export declare class HttpApp {
    private routes;
    private loggingNamespace;
    private _log?;
    private ctx;
    private serverOptions;
    add<P extends string>(path: P, resource: HttpResource<GetPatternParams<P>>): this;
    setMaxRequestBody(bytes: number): this;
    setLogger(log: Logger): this;
    setLoggingNamespace(ns: string): this;
    setSocketTimeout(ms: number): this;
    private get log();
    listen(port?: number | string): Promise<ListeningServer>;
    private handle;
    private handleResource;
    private handleError;
    private handleSystemError;
}
//# sourceMappingURL=app.d.ts.map