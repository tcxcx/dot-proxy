type GetPatternParamNames<P> = P extends `${string}{${infer Rest}` ? Rest extends `${infer Name}}${infer End}` ? Name | GetPatternParamNames<End> : never : never;
export type GetPatternParams<P> = {
    [K in GetPatternParamNames<P>]: string;
};
export declare class PathPattern {
    private regex;
    private names;
    constructor(pattern: string);
    match(path: string): Record<string, string> | undefined;
    private toParams;
}
export {};
//# sourceMappingURL=path-pattern.d.ts.map