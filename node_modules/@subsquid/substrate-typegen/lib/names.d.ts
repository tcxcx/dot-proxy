import { RuntimeDescription, Ti, Type } from '@subsquid/substrate-runtime/lib/metadata';
/**
 * Assign names to types
 */
export declare function assignNames(d: RuntimeDescription): Map<Ti, string>;
export declare class Names {
    private types;
    private assignment;
    private assigned;
    private reserved;
    private aliases;
    constructor(types: Type[]);
    assign(ti: Ti, name: string): void;
    private isFree;
    reserve(name: string): void;
    alias(ti: Ti, name: string): void;
    getAssignment(): Map<Ti, string>;
}
/**
 * Derive "the best" name from the path.
 */
export declare function deriveName(type: Type): string | undefined;
export declare function needsName(types: Type[], ti: Ti): boolean;
//# sourceMappingURL=names.d.ts.map