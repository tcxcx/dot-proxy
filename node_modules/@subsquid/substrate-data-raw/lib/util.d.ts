import { Qty, RuntimeVersionId } from './interfaces';
export declare function qty2Int(qty: Qty): number;
export declare function toQty(n: number): Qty;
export declare function runtimeVersionEquals(a: RuntimeVersionId, b: RuntimeVersionId): boolean;
export interface PrevItem<T> {
    height: number;
    value: T;
}
export declare class Prev<T> {
    private maxSize;
    private items;
    constructor(maxSize?: number);
    get(height: number): T | undefined;
    getItem(height: number): PrevItem<T> | undefined;
    set(height: number, value: T): void;
    private rollbackTo;
}
//# sourceMappingURL=util.d.ts.map