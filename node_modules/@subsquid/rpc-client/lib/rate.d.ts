export declare class RateMeter {
    readonly windowSize: number;
    readonly slotTime: number;
    private window;
    private time;
    constructor(windowSize?: number, slotTime?: number);
    inc(count: number, now?: number): void;
    getRate(now?: number): number;
    private toTime;
}
//# sourceMappingURL=rate.d.ts.map